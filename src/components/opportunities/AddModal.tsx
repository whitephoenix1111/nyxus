// src/components/opportunities/AddModal.tsx
// FILE NÀY ĐÃ BỎ LOGIC — xem REFACTOR.md
// Tạo opportunity mới đi qua POST /api/leads (LeadModal) hoặc POST /api/opportunities (AddDealModal).
// Component này là dead code từ trước refactor, không có caller nào mount nó.
// Giữ file rỗng để tránh broken import nếu có reference chưa tìm thấy.

export {};
